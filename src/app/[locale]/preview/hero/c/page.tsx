import { HeroC } from './HeroC';

export const metadata = {
  title: 'hero c — comparison engine · small p club',
  robots: { index: false, follow: false },
};

export default function HeroCPage() {
  return (
    <main>
      <HeroC />
    </main>
  );
}
