import { HeroA } from './HeroA';

export const metadata = {
  title: 'hero a — mess-werkzeug zerbricht · small p club',
  robots: { index: false, follow: false },
};

export default function HeroAPage() {
  return (
    <main>
      <HeroA />
    </main>
  );
}
