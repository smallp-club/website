import { SpectatorEye } from '@/components/sections/SpectatorEye';

export const metadata = {
  title: '404 — spectator-flip (Preview)',
  robots: { index: false, follow: false },
};

export default function PreviewSpectator() {
  return <SpectatorEye />;
}
