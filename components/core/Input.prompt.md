Text field with a quiet, obround-leaning form and a calm deep-turquoise focus ring.

```jsx
<Input label="Email" type="email" placeholder="you@example.com" helper="No spam — just facts." />
<Input shape="pill" placeholder="join the club" />
```

Use `shape="pill"` for inline signup fields. Set `invalid` to switch border + helper to the sienna signal color.
